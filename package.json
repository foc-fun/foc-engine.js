{
  "name": "foc-engine-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build:core && npm run build:react",
    "build:core": "npm run build --workspace=@foc-engine/core",
    "build:react": "npm run build --workspace=@foc-engine/react",
    "clean": "npm run clean --workspaces",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  },
  "author": "",
  "license": "MIT",
  "description": "FOC Engine monorepo for Starknet dApps"
}